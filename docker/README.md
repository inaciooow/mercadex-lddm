Arquivos auxiliares de Docker podem ser adicionados aqui (scripts de init, proxies, etc.).

O Dockerfile da aplicação fica em `backend/Dockerfile`.
O Compose da stack local está na raiz: `docker-compose.yml`.
